# 前端（React/Vite）

- 安装：`cd frontend && npm install`
- 开发：`npm run dev`（默认 `http://localhost:5173/`）
- 构建：`npm run build`（产物在 `frontend/dist/`）
- 环境：`frontend/.env.development` 设置 `VITE_API_URL`、`VITE_FACTORY_ADDRESS`
- 功能：连接钱包、创建拍卖、用 ETH/用 ERC20 出价、结束与结算、只读状态展示与刷新
